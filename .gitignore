node_modules/

# runtime database — regenerated by src/db/schema.js + seed.js on startup
remoteifes-server/data/*.db
remoteifes-server/data/*.db-shm
remoteifes-server/data/*.db-wal

# secrets — copy .env.example to .env locally instead
.env
*.env
!.env.example

# OS / editor
.DS_Store
Thumbs.db
